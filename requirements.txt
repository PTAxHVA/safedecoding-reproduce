# ==========================================================================
# SafeDecoding - requirements (đã PIN version để chạy được trong venv sạch)
# --------------------------------------------------------------------------
# File gốc để 'datasets'/'accelerate' KHÔNG pin -> pip kéo bản mới nhất,
# lệch với transformers==4.28.1 (datasets 3.x/5.x + huggingface_hub mới bỏ
# 'cached_download' -> ImportError lúc load_dataset). Mình pin về bộ version
# ăn khớp nhau (giữa 2023) đã test import OK. Xem README.md mục "Đã sửa gì".
#
# PyTorch: KHÔNG pin cứng build CUDA ở đây. Cài torch khớp CUDA của GPU TRƯỚC
# (xem README), rồi mới 'pip install -r requirements.txt'. Bản tham chiếu: 2.1.2.
# ==========================================================================

transformers==4.28.1
tokenizers==0.13.3          # đi kèm transformers 4.28.1 (pin cho chắc)
huggingface_hub==0.16.4     # PIN: bản mới bỏ cached_download -> vỡ datasets/transformers cũ
datasets==2.14.6            # PIN: 2.14.x ăn khớp transformers 4.28.1 + hub 0.16
pyarrow==12.0.1             # PIN: datasets 2.14 cần pyarrow.PyExtensionType (pyarrow>=16 đã bỏ -> AttributeError)
accelerate==0.25.0          # PIN: tránh accelerate 1.x quá mới
numpy==1.26.4               # PIN: numpy<2 (numpy 2.x vỡ torch/transformers cũ)

trl==0.7.4                  # CHỈ dùng cho finetune.py (train expert) — KHÔNG cần để chạy defense.py
fschat==0.2.20
ml_collections
tenacity==8.2.3
boto3==1.34.23
openai==1.9.0
google-generativeai==0.8.6 # cho gemini_judge.py (chấm Harmful Score bằng Gemini); 0.8.6 đã test import OK
anthropic                  # optional
matplotlib
sentencepiece

-e ./peft                  # peft BẢN BUNDLE (đã vá adapter_names cho generate) — BẮT BUỘC dùng bản này
-e ./just_eval             # chỉ cần cho Utility (Just-Eval); bỏ qua được nếu chỉ đo DSR
